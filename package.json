{
  "name": "notes",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-rss.js && node scripts/generate-sitemap.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "terser": "^5.27.0"
  }
}
